import React, { useState } from "react";
import Market from "./Market";

interface PropTypes {
    name: string;
    ratio: number;
}

const MarketsContainer = (props: PropTypes) => {


    return (
        <>

        </>
    );
};

export default MarketsContainer;
